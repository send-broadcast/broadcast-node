/**
 * Kept in sync with package.json by test/package.test.ts — a User-Agent that
 * lies about its version is worse than no User-Agent, because server-side
 * client attribution silently attributes to the wrong release.
 */
export const VERSION = '0.1.0';
